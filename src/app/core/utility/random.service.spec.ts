/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RandomService } from './random.service';

describe('Service: Random', () => {
  let randomService: RandomService;

  beforeEach(() => {
    // TestBed.configureTestingModule({
    //   providers: [RandomService]
    // });
    // randomService = new RandomService();
  });

  // xit('should exist', inject([RandomService], (service: RandomService) => {
  //   expect(service).toBeTruthy();
  // }));

  // xit('should return an int between 5 & 10', inject([RandomService], (service: RandomService) => {
  //   const int = service.getRandomInt(5, 10);
  //   expect(int >= 5 && int <= 10).toBe(true);
  // }));

  // xit('should return an boolean', inject([RandomService], (service: RandomService) => {
  //   const bool = service.getRandomBool();
  //   expect(bool).toEqual(jasmine.any(Boolean));
  // }));

  // xit('should return a string with two words', inject([RandomService], (service: RandomService) => {
  //   const name = service.getRandomName();
  //   const nameArray = name.splxit(' ');
  //   expect(nameArray.length).toEqual(2);
  // }));

  // xit('should return a sentence with between 12 & 18 words', inject([RandomService], (service: RandomService) => {
  //   const sentence = service.getRandomSentence();
  //   const sentenceCount = sentence.splxit(' ').length;
  //   expect(sentenceCount >= 12 && sentenceCount <= 18).toBe(true);
  // }));

  // xit('should return a paragraph with between 3 & 7 sentences', inject([RandomService], (service: RandomService) => {
  //   const paragraph = service.getRandomParagraph();
  //   const paragraphCount = paragraph.splxit('.').length;
  //   expect(paragraphCount >= 2 && paragraphCount <= 8).toBe(true);
  // }));

  // xit('should return a photo with specified dimensions', inject([RandomService], (service: RandomService) => {
  //   const photoUrl = service.getRandomPhoto(250, 250);
  //   expect(photoUrl).toEqual(`http://placehold.it/250x250`);
  // }));

  // xit('should return a date of this year', inject([RandomService], (service: RandomService) => {
  //   const date = service.getRandomDate();
  //   expect(date.getFullYear()).toEqual(new Date().getFullYear());
  // }));

  // xit('should return a photo with specified dimensions', inject([RandomService], (service: RandomService) => {
  //   const googleUrl = service.getRandomGoogleUrl();
  //   expect(googleUrl.indexOf('www.google.com') > -1).toBe(true);
  // }));
});
